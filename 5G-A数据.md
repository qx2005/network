新建切片：



切片显示名： **机械臂协同 uRLLC 切片**



描述： **用于产线末端装箱与码垛机械臂的高精度同步控制，要求极低时延与高可靠性，防止动作脱节打碎酒瓶。**



SST： **2 — uRLLC** (高可靠低时延通信)



SD (可选)： **020666**



默认 DNN： **dnn-robot.private**



允许 LADN： **开启**



QoS 参数 (注：工控指令数据量小，但要求绝对保障无延迟)



上行/下行 GBR (Mbps)： **20 / 20**



上行/下行 MBR (Mbps)： **50 / 50**



AMBR (Mbps)： **200**



5QI： **82** (针对离散自动化等低延迟场景)



ARP / 抢占档位： **高** (最高优先级，资源不足时抢占其他切片资源)



成员与分组



成员分组 ID： **grp-robot-arms**







连接新终端：


别名： **1号线-高速机械臂A**



SUPI： **imsi-460001234560001**



IMEISV (可选)： **867400012345671**



网络与状态：



切片 ID： **slice-vision-embb** (关联方案1的切片)



VN ID (可选)： (留空，因为只走 IP 路由，无需大二层)



IP (可选)： **10.45.1.55**



RRC 状态： **RRC\_CONNECTED** (相机需时刻保持连接传输视频)



信号质量描述： **RSRP -65 dBm**



流量累计 (MB)： **12054** (可随意填一个较大的数字，体现视频流的消耗)



省电模板 (可选)：



绑定省电模板： **禁用** (常电供电设备，不休眠)



注册节点：



节点名：**机械臂控制边缘节点**

(含义：起一个有辨识度的名字，方便后续运维一眼看出这是控制谁的服务器。)



N6 本地端点：**10.10.2.10:2152**

(含义：N6 是 5G 核心网连接外部数据网的接口。这里填写实际部署在您车间机房里，用于控制机械臂的那台边缘服务器的 IP 地址和通信端口。)

路由与探测

DNN 列表 (逗号分隔)：**dnn-robot.private**

(含义：这是最关键的一项！ 这个值必须和您在第一步“新建切片”时给机械臂分配的默认 DNN 完全一致。这样网络才知道，带有这个标签的流量应该送到这个本地节点来。)



能力标签 (逗号分隔)：**N6\_BREAKOUT, uRLLC\_Optimized**

(含义：N6\_BREAKOUT 告诉系统这个节点支持流量本地卸载（不出厂区）；uRLLC\_Optimized 标识它做了超低时延优化。)



健康探测 URL：**http://10.10.2.10:8080/health**

(含义：5G-A 平台会定时访问这个网址，看看这台车间服务器是不是还活着、有没有宕机。如果宕机了，系统概览页就会报警。)







新建规则：

规则名： **机械臂协同指令本地极速卸载**



优先级： **20** (注：数值越小越优先。工控指令属于工厂最高优先级，保障其在网络拥堵时也能优先被路由转发。)





目的网段 CIDR (逗号分隔)： **10.45.2.0/24** (注：这是所有机械臂和车间 PLC 所在的专属工控网段。)



源网段 CIDR (可选)： (可**留空**，表示所有发往该目的地的流量都匹配。)



协议： **TCP** (或者选“任意”，具体看工业协议规范。)



端口范围 (逗号分隔)： **102, 4840** (注：这是极其专业的细节！102是西门子S7协议常用端口，4840是OPC UA常用端口。配置这个会让客户觉得非常真实贴切。)



VN ID (可选)： **vn-robot-lan** (注：输入在第三步创建的 5G LAN 组的 ID，精确打击，确保只卸载这个虚拟大二层里的流量。)





动作类型： **本地分流** (注：千万别选丢弃或默认路由，核心能力所在。)



下一跳 (可选)： **10.10.2.10** (注：严格指向在第二步注册的“N6 本地端点”的 IP 地址，告诉网络分流去哪里处理。)



绕过公网： **开启** (绿色) (注：必须开启，硬性要求数据不能暴露在互联网中。)







新建VN组：

显示名： **装箱码垛机械臂 PROFINET 专网**



技术 ID (可留空自动生成)： **vn-robot-lan** (注：建议手动指定，方便我们在终端接入和路由规则里引用它。)



关联切片 ID： **slice-robot-urllc** (注意：非常关键！必须手动填入我们在第一步建好的 uRLLC 切片 ID，千万不要使用默认的视频 eMBB 切片，否则时延无法保障。)



成员配置



成员终端 ID (逗号分隔)： **imsi-460001234560002** (注：这里填入您将要在第五步注册的机械臂通信模组的 SUPI。您可以填入多个终端，把它们组进同一个局域网。)



策略



允许以太网 PDU： **开启** (绿色) (注：极其重要！开启后才能在 5G 链路上透传工业以太网协议。不开启的话只能传 IP 包，PLC 就找不到机械臂了。)



广播策略： **允许** (注：很多工业协议设备上线时会发 ARP 或 DCP 广播寻找控制器，必须设为允许。)



组播策略： **允许** (注：工控指令同步经常使用组播包，需放行。)

