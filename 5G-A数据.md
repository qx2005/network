新建切片：



切片显示名： **机械臂协同 uRLLC 切片**



描述： **用于产线末端装箱与码垛机械臂的高精度同步控制，要求极低时延与高可靠性，防止动作脱节打碎酒瓶。**



SST： **2 — uRLLC** (高可靠低时延通信)



SD (可选)： **020666**



默认 DNN： **dnn-robot.private**



允许 LADN： **开启**



QoS 参数 (注：工控指令数据量小，但要求绝对保障无延迟)



上行/下行 GBR (Mbps)： **20 / 20**



上行/下行 MBR (Mbps)： **50 / 50**



AMBR (Mbps)： **200**



5QI： **82** (针对离散自动化等低延迟场景)



ARP / 抢占档位： **高** (最高优先级，资源不足时抢占其他切片资源)



成员与分组



成员分组 ID： **grp-robot-arms**







连接新终端：


别名： **1号线-高速机械臂A**



SUPI： **imsi-460001234560001**



IMEISV (可选)： **867400012345671**



网络与状态：



切片 ID： **slice-robot-urllc** (机械臂协同 uRLLC 切片，与演示种子一致)



VN ID (可选)： **vn-line1** (与预置机械臂终端同一工业子网)



IP (可选)： **10.45.1.55**



RRC 状态： **RRC\_CONNECTED** (机械臂控制需保持低时延链路)



信号质量描述： **RSRP -65 dBm**



流量累计 (MB)： **12054** (可随意填一个较大的数字，体现控制信令与状态上报)



省电模板 (可选)：



绑定省电模板： **禁用** (常电供电设备，不休眠)



注册节点：



节点名：**机械臂控制边缘节点**

(含义：起一个有辨识度的名字，方便后续运维一眼看出这是控制谁的服务器。)



N6 本地端点：**10.10.2.10:2152**

(含义：N6 是 5G 核心网连接外部数据网的接口。这里填写实际部署在您车间机房里，用于控制机械臂的那台边缘服务器的 IP 地址和通信端口。)

路由与探测

DNN 列表 (逗号分隔)：**dnn-robot.private**

(含义：这是最关键的一项！ 这个值必须和您在第一步“新建切片”时给机械臂分配的默认 DNN 完全一致。这样网络才知道，带有这个标签的流量应该送到这个本地节点来。)



能力标签 (逗号分隔)：**N6\_BREAKOUT, uRLLC\_Optimized**

(含义：N6\_BREAKOUT 告诉系统这个节点支持流量本地卸载（不出厂区）；uRLLC\_Optimized 标识它做了超低时延优化。)



健康探测 URL：**http://10.10.2.10:8080/health**

(含义：5G-A 平台会定时访问这个网址，看看这台车间服务器是不是还活着、有没有宕机。如果宕机了，系统概览页就会报警。)







新建规则：

规则名： **机械臂协同指令本地极速卸载**



优先级： **20** (注：数值越小越优先。工控指令属于工厂最高优先级，保障其在网络拥堵时也能优先被路由转发。)





目的网段 CIDR (逗号分隔)： **10.45.2.0/24** (注：这是所有机械臂和车间 PLC 所在的专属工控网段。)



源网段 CIDR (可选)： (可**留空**，表示所有发往该目的地的流量都匹配。)



协议： **TCP** (或者选“任意”，具体看工业协议规范。)



端口范围 (逗号分隔)： **102, 4840** (注：这是极其专业的细节！102是西门子S7协议常用端口，4840是OPC UA常用端口。配置这个会让客户觉得非常真实贴切。)



VN ID (可选)： **vn-line1** (注：输入在第三步创建的 5G LAN 组的 ID，精确打击，确保只卸载这个虚拟大二层里的流量。)





动作类型： **本地分流** (注：千万别选丢弃或默认路由，核心能力所在。)



下一跳 (可选)： **10.10.2.10** (注：严格指向在第二步注册的“N6 本地端点”的 IP 地址，告诉网络分流去哪里处理。)



绕过公网： **开启** (绿色) (注：必须开启，硬性要求数据不能暴露在互联网中。)







新建VN组：

显示名： **装箱码垛机械臂 PROFINET 专网**



技术 ID (可留空自动生成)： **vn-line1** (注：建议手动指定，方便我们在终端接入和路由规则里引用它。)



关联切片 ID： **slice-robot-urllc** (注意：非常关键！必须手动填入我们在第一步建好的 uRLLC 切片 ID，千万不要使用默认的视频 eMBB 切片，否则时延无法保障。)



成员配置



成员终端 ID (逗号分隔)： **dev-arm-01** (注：与演示平台终端 ID 一致；扩展工控局域网时可追加更多 ID。)



策略



允许以太网 PDU： **开启** (绿色) (注：极其重要！开启后才能在 5G 链路上透传工业以太网协议。不开启的话只能传 IP 包，PLC 就找不到机械臂了。)



广播策略： **允许** (注：很多工业协议设备上线时会发 ARP 或 DCP 广播寻找控制器，必须设为允许。)



组播策略： **允许** (注：工控指令同步经常使用组播包，需放行。)




新建切片：

切片显示名： 高速环形线协同 uRLLC 切片

描述： 用于车间高速磁悬浮环形传送带的多动子（穿梭车）同步控制与实时位置反馈，要求极低时延与强确定性，确保动子间距精准控制与防撞。

SST： 2 — uRLLC (高可靠低时延通信)

SD (可选)： 020888 (注：独立切片标识，与机械臂区分开)

默认 DNN： dnn-ringline.private

允许 LADN： 开启

QoS 参数 (注：环形线动子数量多，状态上报极为频繁，对时延抖动要求极其苛刻)

上行/下行 GBR (Mbps)： 30 / 30 (注：动子节点较多，基础保障带宽需适度提高)

上行/下行 MBR (Mbps)： 100 / 100

AMBR (Mbps)： 300

5QI： 82 (注：针对离散自动化、多轴同步等强实时低延迟场景，完美契合环形线)

ARP / 抢占档位： 高 (最高优先级，资源不足时抢占其他普通业务如视频监控的资源)

成员与分组

成员分组 ID： grp-ring-movers

连接新终端：

别名： 环形模块

SUPI： imsi-460001234560101 IMEISV (可选)： 867400012345881

网络与状态：

切片 ID： slice-ringline-urllc (注：必须关联上面新建的环形线专属切片)

VN ID (可选)： vn-ring-lan (注：环形线极度依赖二层网络通信，终端上线即刻加入 5G LAN 大二层网络)

IP (可选)： 10.46.1.11

RRC 状态： RRC_CONNECTED (注：环形小车在高速移动中需不断上报霍尔/编码器位置，必须时刻保持连接)

信号质量描述： RSRP -70 dBm (注：环形线周边可能存在电机电磁干扰，这个数值比较真实)

流量累计 (MB)： 342 (注：控制报文包极小但频次极高，累计流量通常不大，体现工控特性)

省电模板 (可选)：

绑定省电模板： 禁用 (注：导轨常电供电设备或高性能电池设备，运行期间绝不休眠)

注册节点：

节点名：环形线全局协同控制边缘节点
(含义：部署在环形线旁侧机房的边缘服务器，通常运行着集中式的软 PLC 控制器，统筹所有动子的调度。)

N6 本地端点：10.10.3.10:2152
(含义：核心网对接环形线主控服务器的本地化接口 IP 及端口。)

路由与探测

DNN 列表 (逗号分隔)：dnn-ringline.private
(含义：严格匹配切片的 DNN，确保带有 dnn-ringline.private 标签的小车流量，全部卸载到此边缘服务器，不绕行大网。)

能力标签 (逗号分隔)：N6_BREAKOUT, uRLLC_Optimized, L2_Bridging
(含义：新增 L2_Bridging 标签，表明该节点不仅支持 IP 路由，还支持工业二层协议的桥接处理。)

健康探测 URL：http://10.10.3.10:8080/health_ring
(含义：监控环形线总控服务器状态。若宕机，可迅速触发环形线整体安全急停。)

新建规则：

规则名： 环线动子位置反馈极速卸载

优先级： 10 (注：优先级比普通机械臂（20）更高！因为环形小车速度极快，防撞指令的路由转发优先级必须是车间内的 Top 1。)

目的网段 CIDR (逗号分隔)： 10.46.2.0/24 (注：环形线专属工控网段。)

源网段 CIDR (可选)： (留空)

协议： UDP, TCP (注：位置高频上报多用 UDP 以减小开销，配置下发用 TCP。)

端口范围 (逗号分隔)： 34964, 17222 (注：34964 是 PROFINET RPC 端口，17222 是某些 EtherCAT/实时发布订阅协议的常用端口，凸显专业性。)

VN ID (可选)： vn-ring-lan 动作类型： 本地分流 下一跳 (可选)： 10.10.3.10 绕过公网： 开启 (绿色)

新建VN组：

显示名： 高速环形线 PROFINET RT/IRT 专网

技术 ID (可留空自动生成)： vn-ring-lan (注：在终端接入配置中已被引用。)

关联切片 ID： slice-ringline-urllc 成员配置

成员终端 ID (逗号分隔)： imsi-460001234560101, imsi-460001234560102, imsi-460001234560103 (注：填入环形线上多个动子小车模组的 SUPI。将它们放在同一个虚拟局域网内，实现互相“可见”。)

策略

允许以太网 PDU： 开启 (绿色)
(注：极其关键！环形模块的多动子同步（如基于 EtherCAT 或 PROFINET IRT）极度依赖纯二层 MAC 帧通信，开启此项才能在 5G 上透传工业以太网帧。)

广播策略： 允许 (注：主控器需要发送广播帧来发现新加入环形轨道的动子小车。)

组播策略： 允许 (注：主控器通常通过组播方式，在同一个时钟周期内向所有动子下发同步时钟和全局启停指令，必须放行组播。)




新建切片：

切片显示名： 高精度灌装注水机 uRLLC 切片

描述： 用于产线高精度饮料/药液注水阀门与 PLC 的微秒级同步控制，以及流量计高频数据回传，确保阀门开闭动作极致精准，杜绝溢流与灌装量偏差。

SST： 2 — uRLLC (高可靠低时延通信)

SD (可选)： 020999 (注：独立切片标识，与机械臂及其他设备隔离，保障灌装核心业务)

默认 DNN： dnn-filler.private

允许 LADN： 开启

QoS 参数 (注：注水机的传感器数据和阀门控制指令包极小，但对送达的“准时性/低抖动”要求极高)

上行/下行 GBR (Mbps)： 10 / 10 (注：控制报文流量不大，10M 专属保障带宽已足够应对百兆以太网级的工控流量)

上行/下行 MBR (Mbps)： 30 / 30

AMBR (Mbps)： 100

5QI： 82 (注：专为离散自动化、流体控制等低延迟、高稳定性场景设计)

ARP / 抢占档位： 高 (最高优先级，灌装过程不能被打断)

成员与分组

成员分组 ID： grp-liquid-fillers

连接新终端：

别名： 3号线-高精度注水控制阀#A

SUPI： imsi-460001234560201 IMEISV (可选)： 867400012345991

网络与状态：

切片 ID： slice-filler-urllc (注：必须关联上方新建的灌装专属切片)

VN ID (可选)： vn-filler-lan (注：接入 5G LAN 大二层，方便 PLC 直接扫描 MAC 地址找到阀门岛或 IO 模块)

IP (可选)： 10.47.1.15

RRC 状态： RRC_CONNECTED (注：注水期间流量计需毫秒级回传数据判定是否满瓶，必须时刻保持连接)

信号质量描述： RSRP -68 dBm (注：灌装车间往往有大量不锈钢储罐和液体管道，信号有一定衰减，此数值贴近真实车间环境)

流量累计 (MB)： 56 (注：典型的传感器与阀门控制流量特征——包小、高频、总流量极低)

省电模板 (可选)：

绑定省电模板： 禁用 (注：生产关键设备，绝不休眠)

注册节点：

节点名：灌装液位检测与阀门控制边缘网关
(含义：部署在灌装机旁侧机房的边缘计算节点，处理液位视觉或高频流量传感器的反馈，并直接下发关阀指令。)

N6 本地端点：10.10.4.10:2152
(含义：核心网对接灌装主控 PLC 或边缘网关的本地化接口。)

路由与探测

DNN 列表 (逗号分隔)：dnn-filler.private
(含义：严格匹配，确保带有 dnn-filler.private 标签的传感流量，就近卸载到灌装边缘网关进行毫秒级判决处理。)

能力标签 (逗号分隔)：N6_BREAKOUT, uRLLC_Optimized, L2_Bridging
(含义：支持流量不出厂区卸载、超低时延优化，以及工业二层协议桥接。)

健康探测 URL：http://10.10.4.10:8080/health_filler
(含义：监控边缘总控状态。若发生网络断开，系统触发安全机制，所有阀门默认弹簧复位关闭，防止跑水。)

新建规则：

规则名： 注水启停指令与传感回传极速卸载

优先级： 15 (注：优先级介于防撞环形线(10)和普通机械臂(20)之间。因为一旦延迟会导致液体溢出造成浪费和清洗成本，属于高优工控级别。)

目的网段 CIDR (逗号分隔)： 10.47.2.0/24 (注：灌装机及相关流体控制设备的专属网段。)

源网段 CIDR (可选)： (留空)

协议： TCP, UDP (注：Modbus UDP/TCP 或 PROFINET IO 都可能用到。)

端口范围 (逗号分隔)： 502, 102 (注：细节！502 是流体自动化控制中极为常见的 Modbus TCP 协议标准端口，用于泵阀控制；102 应对西门子 S7 体系。这两个端口一出，即显流程工业专业感。)

VN ID (可选)： vn-filler-lan 动作类型： 本地分流 下一跳 (可选)： 10.10.4.10 绕过公网： 开启 (绿色)

新建VN组：

显示名： 高精度灌装线 Modbus/PROFINET 融合专网

技术 ID (可留空自动生成)： vn-filler-lan 关联切片 ID： slice-filler-urllc 成员配置

成员终端 ID (逗号分隔)： imsi-460001234560201, imsi-460001234560202 (注：这里可以填入多个终端，比如0201是“控制阀门”，0202是“流量计/称重传感器”，让它们在同一个二层专网里极速对话。)

策略

允许以太网 PDU： 开启 (绿色)
(注：流体控制中的分布式 I/O 模块或变频器很多基于纯二层通信，开启后支持透传。)

广播策略： 允许 (注：PLC 寻找新接入的流量计、变频器等从站设备需要发 ARP/DCP 广播。)

组播策略： 允许 (注：如果是多排注水头同时灌装，主控往往发送组播指令让一排 8 个阀门在同一毫秒打开，必须放行组播。)




新建切片：

切片显示名： 高速旋转供料协同 uRLLC 切片

描述： 用于产线前端星轮/离心供料盘的伺服电机高速相位同步，以及光电/视觉检漏的高频状态回传，保障高速上料过程严丝合缝，杜绝卡料与缺件。

SST： 2 — uRLLC (高可靠低时延通信)

SD (可选)： 020777 (注：独立切片标识，专属保障前端供料这个“产线咽喉”环节)

默认 DNN： dnn-rotary-feeder.private

允许 LADN： 开启

QoS 参数 (注：旋转供料涉及多轴伺服电机的微秒级插补指令，要求极低的网络抖动)

上行/下行 GBR (Mbps)： 15 / 15 (注：包含高速光电开关状态、电机编码器反馈及少量辅助判定数据，15M 专属带宽游刃有余)

上行/下行 MBR (Mbps)： 40 / 40

AMBR (Mbps)： 150

5QI： 82 (注：运动控制与离散自动化专属极低延迟 5QI 标识)

ARP / 抢占档位： 高 (供料是整条产线的源头，一旦卡顿全线停机，必须赋予最高抢占权)

成员与分组

成员分组 ID： grp-rotary-feeders

连接新终端：

（注：控制台拓扑库选项 **「旋转供料模块」** 与本段为同一演示方案；Agent 将按本节字面量落参。）

别名： 2号线-高速星轮理料盘#01

SUPI： imsi-460001234560301 IMEISV (可选)： 867400012345331

网络与状态：

切片 ID： slice-rotary-urllc (注：严格关联上面新建的供料专属切片)

VN ID (可选)： vn-rotary-lan (注：接入 5G LAN，以便主控 PLC 直接通过二层网络向伺服驱动器下发运动控制帧)

IP (可选)： 10.48.1.22

RRC 状态： RRC_CONNECTED (注：设备在运转期间，电机编码器数据需保持毫秒级上报，必须维持常态连接)

信号质量描述： RSRP -62 dBm (注：通常部署在开阔的产线前端，无过多遮挡，信号质量较好)

流量累计 (MB)： 128 (注：多轴伺服同步报文极为密集，尽管单包小，但长期累计流量比普通液位传感器略大)

省电模板 (可选)：

绑定省电模板： 禁用 (注：高速旋转电机绝不休眠)

注册节点：

节点名：供料相位同步与检测边缘网关
(含义：部署在供料机附近的边缘控制器，专门负责解析理料盘的转角相位，并与主线的速度进行闭环匹配计算。)

N6 本地端点：10.10.5.10:2152
(含义：核心网对接该供料边缘网关的本地化 N6 接口。)

路由与探测

DNN 列表 (逗号分隔)：dnn-rotary-feeder.private
(含义：所有打上 dnn-rotary-feeder.private 标签的伺服与传感流量，均卸载到该节点处理，不占用厂区核心环网带宽。)

能力标签 (逗号分隔)：N6_BREAKOUT, uRLLC_Optimized, L2_Bridging
(含义：运动控制极度依赖二层透传，必须打上 L2_Bridging 标签支持工业大二层。)

健康探测 URL：http://10.10.5.10:8080/health_rotary
(含义：实时探测网关存活状态。若异常，立即触发供料电机急停，防止物料堆积挤压损坏设备。)

新建规则：

规则名： 多轴伺服运动控制极速卸载

优先级： 18 (注：优先级极高，仅次于防撞(10)和防溢流(15)。因为供料相位的毫秒级错位就会导致机械碰撞或卡瓶。)

目的网段 CIDR (逗号分隔)： 10.48.2.0/24 (注：供料区伺服驱动器和传感器的专属网段。)

源网段 CIDR (可选)： (留空)

协议： UDP, TCP

端口范围 (逗号分隔)： 2222, 34964 (注：硬核细节！2222 是 EtherNet/IP (CIP Motion) 隐式消息常用的 UDP 端口，专用于运动控制；34964 是 PROFINET 端口。涵盖了欧美系最主流的高端伺服协议。)

VN ID (可选)： vn-rotary-lan 动作类型： 本地分流 下一跳 (可选)： 10.10.5.10 绕过公网： 开启 (绿色)

新建VN组：

显示名： 旋转供料运动控制 CIP Motion/PN 专网

技术 ID (可留空自动生成)： vn-rotary-lan 关联切片 ID： slice-rotary-urllc 成员配置

成员终端 ID (逗号分隔)： imsi-460001234560301, imsi-460001234560302 (注：0301可以是“星轮伺服驱动器”，0302可以是“进料口防堵塞光电传感器集群”，两者在同一二层网络内极速联动。)

策略

允许以太网 PDU： 开启 (绿色)
(注：必须开启！多轴电机的等时同步（IRT / CIP Motion）完全建立在纯二层以太网帧的基础之上，不支持 IP 路由穿透。)

广播策略： 允许 (注：用于主站发现新上电的伺服从站。)

组播策略： 允许 (注：极其重要。高端运动控制通常使用组播 (Multicast) 来确保主站的一个同步时钟信号能被多个伺服轴（如拨料盘和传送带）在同一个纳秒级周期内接收，实现完美插补联动。)




新建切片：

切片显示名： 高速物料推送协同 uRLLC 切片

描述： 用于产线高速分拣与剔除环节的推杆气缸、直线电机与光电传感器的微秒级 I/O 控制，确保剔除或分流动作极致精准，防止错漏或挤压损坏物料。

SST： 2 — uRLLC (高可靠低时延通信)

SD (可选)： 020555 (注：独立切片标识，专属保障高速分拣与剔除这一高敏感度环节)

默认 DNN： dnn-pusher.private

允许 LADN： 开启

QoS 参数 (注：推送模块的 I/O 触发指令数据量极小（仅几个字节），但要求绝对的“零丢包、零延迟”）

上行/下行 GBR (Mbps)： 10 / 10 (注：纯工业 I/O 控制信令，10M 专属保障带宽绰绰有余)

上行/下行 MBR (Mbps)： 25 / 25

AMBR (Mbps)： 100

5QI： 82 (注：契合离散自动化中针对高速 I/O 响应的极低延迟标准)

ARP / 抢占档位： 高 (注：最高优先级。剔除次品是质量控制的最后一道防线，指令绝不能被排队)

成员与分组

成员分组 ID： grp-material-pushers

连接新终端：

（注：控制台拓扑库选项 **「物料推送模块」** 与本段为同一演示方案；Agent 将按本节字面量落参。）

别名： 4号线-高速分拣剔除推杆#A

SUPI： imsi-460001234560401 IMEISV (可选)： 867400012345441

网络与状态：

切片 ID： slice-pusher-urllc (注：严格关联上面新建的推送专属切片)

VN ID (可选)： vn-pusher-lan (注：接入 5G LAN 大二层，实现 PLC 与分布式 I/O 模块的底层直连)

IP (可选)： 10.49.1.30

RRC 状态： RRC_CONNECTED (注：生产期间，到达检测位到触发推杆的响应时间通常不足 50ms，终端必须时刻处于连接激活状态)

信号质量描述： RSRP -66 dBm (注：符合典型车间内部署环境的信号强度)

流量累计 (MB)： 45 (注：极为典型的分布式 I/O 流量特征——数据包极小，哪怕高频工作一天，累计流量也非常低)

省电模板 (可选)：

绑定省电模板： 禁用 (注：随时待命的高速执行机构，严禁休眠)

注册节点：

节点名：物料分流与剔除控制边缘网关
(含义：部署在分拣线旁侧，负责接收前序视觉/传感器的判定结果，并向推杆电磁阀下发硬实时触发指令的边缘设备。)

N6 本地端点：10.10.6.10:2152
(含义：核心网对接该分流控制边缘网关的本地化 N6 接口。)

路由与探测

DNN 列表 (逗号分隔)：dnn-pusher.private
(含义：严格匹配，确保带有此标签的高频触发 I/O 流量在车间本地就近卸载，不绕行厂区大网。)

能力标签 (逗号分隔)：N6_BREAKOUT, uRLLC_Optimized, L2_Bridging
(含义：支持本地卸载、超低时延优化，以及对于 PROFINET IO 等二层协议的支持。)

健康探测 URL：http://10.10.6.10:8080/health_pusher
(含义：实时探测网关状态。若断网，立即触发传送带减速或停机，防止未经剔除判定的物料流向下一道工序。)

新建规则：

规则名： 高速推杆 I/O 触发指令极速卸载

优先级： 12 (注：优先级极高，仅次于环形线的物理防撞(10)。因为漏剔次品会导致整批产品面临客户退货的严重客诉风险。)

目的网段 CIDR (逗号分隔)： 10.49.2.0/24 (注：分拣区气缸阀门岛及传感器的专属网段。)

源网段 CIDR (可选)： (留空)

协议： UDP, TCP

端口范围 (逗号分隔)： 34964, 502 (注：34964 是 PROFINET IO 实时通信端口，502 是 Modbus TCP 端口。很多气动阀门岛（如Festo、SMC）使用这两种协议接收 PLC 的电磁阀开闭信号。)

VN ID (可选)： vn-pusher-lan 动作类型： 本地分流 下一跳 (可选)： 10.10.6.10 绕过公网： 开启 (绿色)

新建VN组：

显示名： 高速推杆阀门岛 PROFINET/Modbus 专网

技术 ID (可留空自动生成)： vn-pusher-lan 关联切片 ID： slice-pusher-urllc 成员配置

成员终端 ID (逗号分隔)： imsi-460001234560401, imsi-460001234560402 (注：0401可以是“推杆阀门岛”，0402可以是上游的“光电到达传感器”，它们在同一个 5G LAN 内实现纳秒级协同联动。)

策略

允许以太网 PDU： 开启 (绿色)
(注：必须开启！工业现场的分布式 I/O（如控制气缸的阀门岛）大量使用二层 MAC 帧直通通信以压榨最后几毫秒的延迟，开启此项才能在 5G 上透传。)

广播策略： 允许 (注：用于 PLC 主站寻找并配置新接入的 I/O 子站设备。)

组播策略： 允许 (注：在高速分流场景中，有时需要同时触发气缸推杆、皮带转向以及报警灯，组播可确保多个 I/O 节点同时响应动作。)




新建切片：

切片显示名： 视觉定位与伺服推送协同 uRLLC 切片

描述： 用于产线精细装配或剔除环节的“视觉定位+伺服推杆”闭环控制。既需要大上行带宽回传高帧率定位图像，又需要极低时延下发毫秒级伺服插补指令，确保物料被精准推送至微米级指定位置。

SST： 2 — uRLLC (高可靠低时延通信)

SD (可选)： 020444 (注：独立切片标识，专属保障视觉与控制融合业务)

默认 DNN： dnn-pos-pusher.private

允许 LADN： 开启

QoS 参数 (注：定位推送模块是典型的“非对称高并发”业务，上行传图大带宽，下发指令极低时延)

上行/下行 GBR (Mbps)： 50 / 15 (注：上行 50M 保障工业相机或 3D 线激光轮廓仪的连续图传；下行 15M 保障伺服控制)

上行/下行 MBR (Mbps)： 150 / 40

AMBR (Mbps)： 300

5QI： 80 (注：5QI=80 是专为要求低延迟但同时伴随较高带宽负载的“增强现实/工业视觉协同”场景设计的标准，非常贴合“看+动”的定位推送需求)

ARP / 抢占档位： 高 (注：高优先级，防止因为其他车间视频下载占用带宽导致定位判定超时)

成员与分组

成员分组 ID： grp-pos-pushers

连接新终端：

（注：控制台拓扑库选项 **「定位推送模块」** 与本段为同一演示方案；Agent 将按本节字面量落参。）

别名： 5号线-高精定位伺服推杆#01

SUPI： imsi-460001234560501 IMEISV (可选)： 867400012345551

网络与状态：

切片 ID： slice-pos-pusher-urllc (注：严格关联上面新建的定位推送专属切片)

VN ID (可选)： vn-pos-pusher-lan (注：接入 5G LAN，实现“相机-边缘计算(软PLC)-伺服驱动器”在同一二层网络内闭环)

IP (可选)： 10.50.1.45

RRC 状态： RRC_CONNECTED (注：高频视觉定位期间不允许任何连接重建时延，必须锁定在连接态)

信号质量描述： RSRP -64 dBm

流量累计 (MB)： 4850 (注：相比于纯 I/O 推杆的 45MB，由于加入了高频次的 2D/3D 定位图像回传，累计流量会显著呈几何级增加，这体现了极强的业务真实感)

省电模板 (可选)：

绑定省电模板： 禁用 注册节点：

节点名：视觉定位与伺服推送协同
(含义：部署在工位旁侧的 MEC 节点。它既运行着 AI 视觉定位算法，又运行着下发推送指令的软 PLC，实现端到端的极速闭环。)

N6 本地端点：10.10.7.10:2152

路由与探测

DNN 列表 (逗号分隔)：dnn-pos-pusher.private
(含义：严格匹配，确保海量定位图片和敏感的伺服指令绝对不出厂区。)

能力标签 (逗号分隔)：N6_BREAKOUT, uRLLC_Optimized, Edge_Computing_Vision
(含义：增加 Edge_Computing_Vision 标签，暗示该节点具备异构算力（GPU+CPU），能处理定位图像的就地解算。)

健康探测 URL：http://10.10.7.10:8080/health_pos
(含义：实时探测网关状态。若边缘视觉算法容器宕机，推杆立刻锁死，避免“盲推”造成昂贵物料损毁。)

新建规则：

规则名： 视觉定位流与伺服指令本地融合卸载

优先级： 14 (注：优先级极高，夹在防撞/防溢流与纯机械臂之间。兼顾安全与高节拍生产。)

目的网段 CIDR (逗号分隔)： 10.50.2.0/24 (注：视觉相机和伺服推杆所在的工控网段。)

源网段 CIDR (可选)： (留空)

协议： UDP, TCP

端口范围 (逗号分隔)： 3956, 34964 (注：极其硬核的组合！3956 是 GigE Vision (千兆以太网视觉) 协议的默认 UDP 端口，用于工业相机传图；34964 是 PROFINET 端口，用于下发推送指令。这充分证明了这是一个真正的“光机电算”融合模块。)

VN ID (可选)： vn-pos-pusher-lan 动作类型： 本地分流 下一跳 (可选)： 10.10.7.10 绕过公网： 开启 (绿色)

新建VN组：

显示名： 定位推送 GigE/PROFINET 融合专网

技术 ID (可留空自动生成)： vn-pos-pusher-lan 关联切片 ID： slice-pos-pusher-urllc 成员配置

成员终端 ID (逗号分隔)： imsi-460001234560501, imsi-460001234560502 (注：501 可以是“高精度伺服推杆”，502 则是顶部的“GigE 工业定位相机”。)

策略

允许以太网 PDU： 开启 (绿色)
(注：必须开启。 GigE Vision 协议和 PROFINET 在工业现场均高度依赖纯以太网 MAC 层的直接通信，开启后消除 IP 路由封装拆解的时延。)

广播策略： 允许 (注：主要用于视觉软件自动发现网段内新接入的工业相机。)

组播策略： 允许 (注：GigE Vision 通常需要组播功能，以便多个边缘计算节点（如一个做定位，一个做表面缺陷检测）可以同时订阅同一台相机画面的多播组，而无需消耗相机额外的带宽。)





新建切片：

切片显示名： 工业相机大上行切片

描述： 用于产线高速 AI 表面缺陷检测与 3D 轮廓扫描。工业相机需持续上传高帧率、无压缩的图像，极度消耗上行带宽，要求网络具备极强的上行吞吐能力与低丢包率。

SST： 1 — eMBB (增强型移动宽带 - 注：视觉类业务通常归属 eMBB，但 5G-A 会对其进行“大上行”特化)

SD (可选)： 010888 (注：独立的视觉大流量切片标识，防止其海量数据冲垮其他工控指令切片)

默认 DNN： dnn-vision.private

允许 LADN： 开启

QoS 参数 (注：这是整套配置的灵魂所在！工业相机的流量是极其极端的“重上行、轻下行”不对称模型)

上行/下行 GBR (Mbps)： 150 / 10 (注：上行高达 150M 保障底线！ 工业相机一旦跑起来，巨大的无压缩图像流需要极高的上行保障；而下行只需 10M 用于相机参数修改或触发指令)

上行/下行 MBR (Mbps)： 300 / 50 (注：上行峰值给到 300Mbps，完美展现 5G-A 的大上行优势)

AMBR (Mbps)： 500

5QI： 80 (注：专为要求极低丢包率和高吞吐量的增强现实/机器视觉场景设计的 5QI 标准)

ARP / 抢占档位： 中 (注：质检虽然重要，但如果网络极度拥堵面临崩溃，保底优先级需让步于控制机械臂防撞的 uRLLC 业务)

成员与分组

成员分组 ID： grp-ai-cameras

连接新终端：

（注：控制台拓扑库选项 **「工业相机模块」** 与本段为同一演示方案；Agent 将按本节字面量落参。）

别名： 工业相机

SUPI： imsi-460001234560601 IMEISV (可选)： 867400012345661

网络与状态：

切片 ID： slice-vision-embb (注：严格关联上面新建的大上行视觉切片)

VN ID (可选)： vn-vision-lan (注：接入视觉专属 5G LAN，以便边缘服务器广播扫描发现工业相机)

IP (可选)： 10.51.1.80

RRC 状态： RRC_CONNECTED (注：产线运行期间，工业相机以高帧率持续传图，必然是持续的连接激活态)

信号质量描述： RSRP -60 dBm (注：通常部署在质检龙门架上，天线对齐较好，信号优良)

流量累计 (MB)： 285,040 (注：工业相机连续拍摄几小时就能产生数百 GB 流量。巨大的累计流量与控制指令极小的流量形成强烈对比，极度真实！)

省电模板 (可选)：

绑定省电模板： 禁用 (注：持续传图，严禁休眠)

注册节点：

节点名：工业相机边缘算力节点
(含义：部署在车间的高性能 MEC 服务器，负责就近接收工业相机的海量图片并运行算法进行处理。)

N6 本地端点：10.10.8.10:2152
(含义：核心网对接车间边缘服务器的本地化接口。)

路由与探测

DNN 列表 (逗号分隔)：dnn-vision.private
(含义：严格匹配，确保海量的视频流/图片流在车间内就近“卸载”给服务器，绝对不能去冲击省市级的骨干大网。)

能力标签 (逗号分隔)：N6_BREAKOUT, Massive_Uplink, Edge_AI_GPU
(含义：凸显该节点是一个专门处理视觉算力的重载节点。)

健康探测 URL：http://10.10.8.10:8080/health_camera
(含义：监控处理节点的状态。)

新建规则：

规则名： 工业相机图像流本地卸载

优先级： 30 (注：优先级低于硬实时工控指令（10~20）。控制信令永远高于视频流数据。)

目的网段 CIDR (逗号分隔)： 10.51.2.0/24 (注：服务器所在的专属网段。)

源网段 CIDR (可选)： (留空)

协议： UDP, TCP

端口范围 (逗号分隔)： 3956, 554, 8081 (注：3956 是 GigE Vision 千兆视觉协议的灵魂端口；554 是 RTSP 视频流标准端口。)

VN ID (可选)： vn-vision-lan 动作类型： 本地分流 下一跳 (可选)： 10.10.8.10 绕过公网： 开启 (绿色)

新建VN组：

显示名： 工业相机专属专网

技术 ID (可留空自动生成)： vn-vision-lan 关联切片 ID： slice-vision-embb 成员配置

成员终端 ID (逗号分隔)： imsi-460001234560601, imsi-460001234560602 (注：可以同时填入多个工业相机的 SUPI。)

策略

允许以太网 PDU： 开启 (绿色)
(注：很多工业相机使用底层纯以太网帧传输图像，开启此项可完美支持。)

广播策略： 允许 (注：视觉软件启动时，必须发送广播包去寻址并唤醒网段内的工业相机。)

组播策略： 允许






新建切片：

切片显示名： 气动伸缩夹爪协同 uRLLC 切片

描述： 用于产线机械臂或桁架末端气动伸缩夹爪的微秒级开闭控制。要求网络具备极低时延与高可靠性，确保抓取和释放时机极其精准，防止物料掉落或夹坏精密部件。

SST： 2 — uRLLC (高可靠低时延通信)

SD (可选)： 020333 (注：独立切片标识，专属保障末端执行器的硬实时控制)

默认 DNN： dnn-gripper.private

允许 LADN： 开启

QoS 参数 (注：夹爪的电磁阀控制指令数据量极小，但要求绝对的“零延迟”，典型的小包高并发场景)

上行/下行 GBR (Mbps)： 10 / 10 (注：纯工业 I/O 控制信令，10M 专属保障带宽绰绰有余)

上行/下行 MBR (Mbps)： 25 / 25

AMBR (Mbps)： 100

5QI： 82 (注：契合离散自动化中针对高速 I/O 和气动阀门响应的极低延迟标准)

ARP / 抢占档位： 高 (注：最高优先级。夹取动作直接关系到产品安全，指令绝不能被排队或丢包)

成员与分组

成员分组 ID： grp-pneumatic-grippers

连接新终端：

（注：控制台拓扑库选项 **「气动伸缩夹爪模块」** 与本段为同一演示方案；Agent 将按本节字面量落参。）

别名： 气动伸缩夹爪

SUPI： imsi-460001234560701 IMEISV (可选)： 867400012345771

网络与状态：

切片 ID： slice-gripper-urllc (注：严格关联上面新建的气动伸缩夹爪专属切片)

VN ID (可选)： vn-gripper-lan (注：接入专属 5G LAN 大二层，实现主控 PLC 与夹爪阀门岛的底层直接通信)

IP (可选)： 10.52.1.25

RRC 状态： RRC_CONNECTED (注：生产节拍紧凑时，夹爪处于随时待命状态，不允许出现网络休眠唤醒带来的额外延迟，必须维持常态连接)

信号质量描述： RSRP -65 dBm (注：符合典型车间内部署环境的信号强度)

流量累计 (MB)： 42 (注：极为典型的气动 I/O 流量特征——数据包极小（有时仅为1个bit的布尔量），哪怕高频工作一天，累计流量也非常低，极度贴合真实业务)

省电模板 (可选)：

绑定省电模板： 禁用 (注：随时待命的高速执行机构，严禁休眠)

注册节点：

节点名：气动伸缩夹爪控制边缘网关
(含义：部署在产线旁侧，负责接收 PLC 逻辑并向夹爪的电磁阀下发硬实时触发指令的边缘设备。)

N6 本地端点：10.10.9.10:2152
(含义：核心网对接该夹爪控制边缘网关的本地化 N6 接口。)

路由与探测

DNN 列表 (逗号分隔)：dnn-gripper.private
(含义：严格匹配，确保带有此标签的高频触发 I/O 流量在车间本地就近卸载。)

能力标签 (逗号分隔)：N6_BREAKOUT, uRLLC_Optimized, L2_Bridging
(含义：支持本地卸载、超低时延优化，以及对于 PROFINET IO 等二层工业协议的支持。)

健康探测 URL：http://10.10.9.10:8080/health_gripper
(含义：实时探测网关状态。若断网，立即触发安全机制，夹爪默认保持当前气压状态，防止突然松开导致物料砸落。)

新建规则：

规则名： 气动伸缩夹爪开闭指令本地卸载

优先级： 16 (注：优先级极高，夹爪的动作时序是工业机器人控制系统中最优先保障的环节之一。)

目的网段 CIDR (逗号分隔)： 10.52.2.0/24 (注：气缸阀门岛及末端传感器的专属网段。)

源网段 CIDR (可选)： (留空)

协议： UDP, TCP

端口范围 (逗号分隔)： 34964, 502 (注：34964 是 PROFINET IO 实时通信端口，502 是 Modbus TCP 端口。工业气动夹爪通常通过阀门岛（如Festo、SMC）接入这两种协议来接收电磁阀信号。)

VN ID (可选)： vn-gripper-lan 动作类型： 本地分流 下一跳 (可选)： 10.10.9.10 绕过公网： 开启 (绿色)

新建VN组：

显示名： 气动伸缩夹爪专属专网

技术 ID (可留空自动生成)： vn-gripper-lan 关联切片 ID： slice-gripper-urllc 成员配置

成员终端 ID (逗号分隔)： imsi-460001234560701, imsi-460001234560702 (注：可包含多个气动伸缩夹爪的通信模组，让它们在同一个 5G LAN 内协同联动。)

策略

允许以太网 PDU： 开启 (绿色)
(注：必须开启！工业现场控制气动夹爪的分布式 I/O 模块大量使用纯二层 MAC 帧直通通信以压榨最后几毫秒的延迟，开启此项才能在 5G 链路上透传底层报文。)

广播策略： 允许 (注：用于 PLC 主站寻找并配置新接入的夹爪 I/O 子站设备。)

组播策略： 允许 (注：在多个气动伸缩夹爪需要同步抓取大型物料（如汽车玻璃、大尺寸面板）时，组播可确保多个夹爪在同一毫秒内同步闭合，防止受力不均扯坏物料。)





新建切片：

切片显示名： 入库升降机协同 uRLLC 切片

描述： 用于立体仓库（AS/RS）入库升降机的垂直运动控制与高精度平层定位。要求极高可靠性与低时延，确保变频器驱动指令精准下发，杜绝越位冲顶或与货架发生碰撞事故。

SST： 2 — uRLLC (高可靠低时延通信)

SD (可选)： 020888 (注：独立切片标识，专属保障仓储物流核心的大型特种设备)

默认 DNN： dnn-elevator.private

允许 LADN： 开启

QoS 参数 (注：升降机涉及重载设备的安全启停和激光测距仪的高频数据反馈，对网络的稳定性要求极高)

上行/下行 GBR (Mbps)： 15 / 15 (注：包含绝对值编码器/激光测距仪数据及变频器控制帧，15M 专属带宽能够提供充足冗余)

上行/下行 MBR (Mbps)： 40 / 40

AMBR (Mbps)： 150

5QI： 82 (注：契合离散自动化与大型传动控制的低延迟标准)

ARP / 抢占档位： 高 (注：最高优先级。涉及重型特种设备的安全运动控制，绝对不容许指令被排队)

成员与分组

成员分组 ID： grp-warehouse-elevators

连接新终端：

别名： 入库升降机

SUPI： imsi-460001234560801 IMEISV (可选)： 867400012345881

网络与状态：

切片 ID： slice-elevator-urllc (注：严格关联上面新建的入库升降机专属切片)

VN ID (可选)： vn-elevator-lan (注：接入专属 5G LAN 大二层，实现仓储 WCS/PLC 主控与升降机变频驱动器的底层直接通信)

IP (可选)： 10.53.1.50

RRC 状态： RRC_CONNECTED (注：升降机在上下穿梭过程中需持续上报实时高度坐标，必须维持常态连接，避免休眠唤醒延迟)

信号质量描述： RSRP -72 dBm (注：极其真实的细节！立体仓库内部全是密集的金属货架，对信号遮挡严重，因此这里的 RSRP 会比开阔产线稍差，体现出实地部署的真实环境)

流量累计 (MB)： 156 (注：比气动夹爪大，因为升降机的驱动器（如 PROFIdrive 报文）会周期性地高频交互状态字和控制字)

省电模板 (可选)：

绑定省电模板： 禁用 (注：重型特种设备随时待命，严禁休眠)

注册节点：

节点名：入库升降机控制边缘网关
(含义：部署在立体库旁侧的控制柜中，负责接收 WCS 系统调度，并向升降电机的变频器下发硬实时运动指令的边缘设备。)

N6 本地端点：10.10.10.10:2152
(含义：核心网对接该升降机控制边缘网关的本地化 N6 接口。)

路由与探测

DNN 列表 (逗号分隔)：dnn-elevator.private
(含义：严格匹配，确保立体库大型设备的工控流量在库区本地就近卸载。)

能力标签 (逗号分隔)：N6_BREAKOUT, uRLLC_Optimized, L2_Bridging
(含义：支持本地卸载、超低时延优化，以及对于大型变频器底层二层通信的支持。)

健康探测 URL：http://10.10.10.10:8080/health_elevator
(含义：实时探测网关状态。若断网，立即触发变频器“安全抱闸 (STO)”，使升降机悬停在当前高度，防止溜车坠落。)

新建规则：

规则名： 入库升降机变频驱动指令本地卸载

优先级： 15 (注：优先级极高。大型升降机重达数吨，惯性极大，其平层刹车指令哪怕延迟几十毫秒都会导致设备对不准货位而发生撞击。)

目的网段 CIDR (逗号分隔)： 10.53.2.0/24 (注：立体库变频器、激光测距仪的专属网段。)

源网段 CIDR (可选)： (留空)

协议： UDP, TCP

端口范围 (逗号分隔)： 34964, 44818 (注：核心专业细节！34964 是 PROFINET 端口，44818 是 EtherNet/IP 端口。在大型物流立库中，SEW、丹佛斯等变频器大量采用这两种协议接收速度和位置控制报文。)

VN ID (可选)： vn-elevator-lan 动作类型： 本地分流 下一跳 (可选)： 10.10.10.10 绕过公网： 开启 (绿色)

新建VN组：

显示名： 入库升降机专属专网

技术 ID (可留空自动生成)： vn-elevator-lan 关联切片 ID： slice-elevator-urllc 成员配置

成员终端 ID (逗号分隔)： imsi-460001234560801, imsi-460001234560802 (注：包含升降机的通信模组以及其上搭载的货叉控制模组，实现垂直运动与水平取货的完美联动。)

策略

允许以太网 PDU： 开启 (绿色)
(注：必须开启。用于透传 PROFIdrive 等依赖二层以太网帧的运动控制报文。)

广播策略： 允许 (注：用于 PLC 主站寻找并配置新接入的变频器或激光测距仪。)

组播策略： 允许 (注：在复杂的多轴立库中，升降轴和行走轴（天车）有时需要组播指令来实现起步和刹车的动态同步，防止塔身摇晃晃动。)





新建切片：

切片显示名： 边缘计算单元协同专属切片

描述： 用于车间柔性部署的无线边缘计算单元。作为产线区域的“数字大脑”，需承载海量多源异构数据（视觉、传感器等）的极速汇聚与本地 AI 解析，同时保障微秒级软 PLC 控制指令的实时下发，是典型的“大带宽+低时延”双叠加高负载场景。

SST： 2 — uRLLC (高可靠低时延通信 - 注：算力节点兼具控制与分析，首保控制不掉线，故选择 uRLLC 并搭配超大带宽)

SD (可选)： 020111 (注：独立切片标识，专属保障车间核心算力大脑)

默认 DNN： dnn-edge-compute.private

允许 LADN： 开启

QoS 参数 (注：边缘计算单元是汇聚节点，流量特征为“重度对称吞吐”，既要接收无数终端的数据，又要与中心云同步大模型)

上行/下行 GBR (Mbps)： 200 / 200 (注：保障带宽极高。确保无论网络多么拥堵，该单元始终有 200M 的骨干通道处理区域内所有设备的数据)

上行/下行 MBR (Mbps)： 1000 / 1000 (注：峰值达到千兆级，完美释放 5G-A 万兆/千兆能力的算力节点专属待遇)

AMBR (Mbps)： 2000

5QI： 80 (注：契合伴随较高带宽负载且要求极低丢包率的复杂工业计算场景)

ARP / 抢占档位： 高 (注：最高优先级！边缘计算单元如果断网，其管辖的整个区域的机器人和相机都会瘫痪，必须拥有全厂最高路权)

成员与分组

成员分组 ID： grp-edge-compute

连接新终端：

（注：控制台拓扑库选项 **「边缘计算单元模块」** 与本段为同一演示方案；Agent 将按本节字面量落参。）

别名： 边缘计算单元

SUPI： imsi-460001234560901 IMEISV (可选)： 867400012345991

网络与状态：

切片 ID： slice-edge-compute (注：严格关联上面新建的边缘计算单元专属切片)

VN ID (可选)： vn-edge-compute-lan (注：边缘计算单元需作为核心节点接入 5G LAN 大二层，统领下属的所有终端设备)

IP (可选)： 10.54.1.100

RRC 状态： RRC_CONNECTED (注：作为算力大脑，数据吞吐永不停止，永远处于长连接状态)

信号质量描述： RSRP -58 dBm (注：边缘计算设备通常配有高性能的工业级 5G CPE 或高增益天线，信号接收能力极佳)

流量累计 (MB)： 1,540,200 (注：约 1.5 TB！这是极其专业的细节。作为汇聚所有相机和传感器数据的中心节点，它的累计流量是天文数字，与普通单一传感器形成强烈的量级对比！)

省电模板 (可选)：

绑定省电模板： 禁用 (注：算力全开的车间大脑，严禁任何形式的休眠)

注册节点：

节点名：边缘计算单元算力分流网关
(含义：用于对接 5G 网络和边缘计算物理硬件（如内置 GPU 的工控机集群）的网关程序。)

N6 本地端点：10.10.11.10:2152
(含义：核心网对接该算力单元的本地化 N6 接口。)

路由与探测

DNN 列表 (逗号分隔)：dnn-edge-compute.private
(含义：严格匹配，确保整条产线的区域数据全部汇聚到此边缘单元进行“端侧处理”，有效减轻厂区核心网的压力。)

能力标签 (逗号分隔)：N6_BREAKOUT, Edge_Computing_GPU, Data_Aggregation
(含义：硬核标签组合！明确告诉系统这是一个具备 GPU 并发算力和海量数据汇聚能力的高级卸载节点。)

健康探测 URL：http://10.10.11.10:8080/health_edge
(含义：实时探测计算单元内部的 Kubernetes 容器或 Docker 服务存活状态。若宕机，立即触发备用主机的热切换。)

新建规则：

规则名： 边缘计算单元 IT/OT 异构数据极速卸载

优先级： 5 (注：全场最高优先级 (数字最小)！因为它是所有控制指令的发源地和所有数据的终点，其路由规则必须最先被匹配执行。)

目的网段 CIDR (逗号分隔)： 10.54.2.0/24 (注：边缘计算微服务集群及数据总线所在的专属网段。)

源网段 CIDR (可选)： (留空)

协议： UDP, TCP

端口范围 (逗号分隔)： 1883, 9092, 4840 (注：展现架构师实力的顶级细节！1883 是 MQTT 物联网关端口；9092 是 Kafka 数据流总线端口；4840 是 OPC UA 工业控制端口。这三个端口一出，完美诠释了边缘计算单元“IT 与 OT 深度融合”的核心定位。)

VN ID (可选)： vn-edge-compute-lan 动作类型： 本地分流 下一跳 (可选)： 10.10.11.10 绕过公网： 开启 (绿色)

新建VN组：

显示名： 边缘计算单元算力协同专属专网

技术 ID (可留空自动生成)： vn-edge-compute-lan 关联切片 ID： slice-edge-compute 成员配置

成员终端 ID (逗号分隔)： imsi-460001234560901 (注：边缘计算单元的通信模组。实际部署时，同网段还会加入受它控制的各种机械臂、相机模组等。)

策略

允许以太网 PDU： 开启 (绿色)
(注：必须开启。边缘计算内部的软 PLC (如 Codesys) 需通过纯二层 MAC 帧直接控制底层的伺服和气缸。)

广播策略： 允许 (注：用于边缘集群内各类微服务 (如 mDNS 协议) 的自动发现与注册。)

组播策略： 允许 (注：极其重要。边缘计算往往采用 ROS2 (基于 DDS) 或其他分布式数据总线，大量依赖组播协议来实现算力节点间的高效主题订阅与数据分发。)
